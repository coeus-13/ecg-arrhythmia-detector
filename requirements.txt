# =============================================================================
# ECG Arrhythmia Detection Pipeline - Requirements
# Target: CPU-only execution (Intel i7-1360P, no CUDA/dedicated GPU)
# Tested with Python 3.10 / 3.11
#
# Install with:
#   python -m venv .venv
#   source .venv/bin/activate        (Linux/Mac)
#   .venv\Scripts\activate           (Windows)
#   pip install -r requirements.txt
# =============================================================================

# --- Core numerical / data stack ---
numpy==1.26.4
pandas==2.2.2
scipy==1.12.0

# --- Signal processing ---
PyWavelets==1.6.0          # pywt - Discrete Wavelet Transform (db4)

# --- Machine learning ---
scikit-learn==1.4.2
imbalanced-learn==0.12.0   # SMOTE + RandomUnderSampler, imblearn Pipeline
joblib==1.3.2              # model persistence (bundled with sklearn but pinned explicitly)

# --- PhysioNet / WFDB access ---
wfdb==4.1.2                # bulk download + parsing of MIT-BIH (mitdb) records

# --- Visualization (for evaluation reports / figures) ---
matplotlib==3.8.3
seaborn==0.13.2

# --- Utilities ---
tqdm==4.66.2                # progress bars during long feature extraction
pyyaml==6.0.1                # config loading
tabulate==0.9.0              # pretty console tables for evaluation reports

# --- Testing ---
pytest==8.1.1
pytest-cov==4.1.0

# =============================================================================
# Explicitly EXCLUDED by project requirements (do not install):
#   torch, tensorflow, keras, jax
# =============================================================================